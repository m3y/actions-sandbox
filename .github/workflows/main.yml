on: [push]

jobs:
  label-validator:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Hello world action step
      id: hello
      uses: m3y/label-validator@test
      with:
        github-token: 'testtoken'
        labels: 'test,test2'
        labeler: 'm3y'
